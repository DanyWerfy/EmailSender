# PDF and Email Automation Tool

This project consists of two Python scripts that work together to automate the generation of personalized PDF proposals and the creation of mass email drafts with those proposals attached.

---

## Features

* **Automated PDF Generation:** Creates a unique PDF for each entry in a CSV file by replacing variables in a Word document template.
* **Mass Email Drafting:** Generates personalized email drafts in your Outlook account, with variables replaced in the subject and body.
* **Dynamic Attachments:** Automatically attaches a company-specific PDF proposal (generated by the first script) and a marketing flyer to each email.
* **Automated Authentication:** Uses an interactive browser to securely connect to the Microsoft Graph API.

---

## Requirements

To get this project up and running, you'll need to have **Python 3.x** installed on your system. All other necessary libraries are listed in the `requirements.txt` file.

---

## Getting Started

Follow these simple steps to set up the project locally.

### 1. Clone the Repository

First, clone the project from its GitHub repository to your local machine:
`git clone [Your Repository URL]`
`cd [Your Project Folder]`

### 2. Create a Virtual Environment

It's a best practice to use a virtual environment to manage project dependencies. This prevents conflicts with other Python projects on your computer.

`python -m venv .venv`

### 3. Activate the Virtual Environment

Activate the virtual environment you just created.

* **On Windows:**
    `.venv\Scripts\activate`

* **On macOS/Linux:**
    `source .venv/bin/activate`

### 4. Install Dependencies

With the virtual environment active, install all the required packages using `pip`.
`pip install -r requirements.txt`

### 5. File and Configuration Setup

This project requires several files and a configuration file to run. Please ensure your project's directory structure matches the following:

├── CreateEmailDrafts.py
├── GeneratePDFs.py
└── data/
├── attachements/
├── inputs/
│   ├── EN_WERFY_Corporate Proposal_[Company name].docx
│   ├── Hotelrez_Marketing flyer.pdf
│   └── Logo.png
├── template/
│   └── emailTemplate.msg
├── Montreal Market Data 2025(Sheet1).csv
└── config.cfg


* **Configuration File:** Create a file named **`config.cfg`** inside the `data` folder and add your Azure Application (client) ID in the following format:

```ini
[azure]
ClientId = your_client_id_here